# Poem Sentiment Analysis

Implemented a natural language processing pipeline to classify the sentiment of poems using the “DistilBERT-base-uncased” model fine-tuned on a curated poetry dataset. Performed data preprocessing, tokenization, model fine-tuning and evaluation in Python.
